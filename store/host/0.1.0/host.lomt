// host/host.lomt — 包入口 (宿主层)。
//
// 包内 6 个模块, 全是**与宿主系统打交道**的东西: 系统调用, fd, argv, 目录, stat, 日志。
// 它们依赖 std (可移植层) —— 方向是单向的, std 不认识 host。
//
// 这里不写 `use std`: 包到 std 的依赖边由 io.lomt 自己的 `use std` 给出 (依赖来自源码,
// 不另行声明 —— docs/168 §3.1)。重复写一遍不会更清楚, 只会多一条同名的边。

module host

use "argv.lomt"
use "dir.lomt"
use "fs.lomt"
use "io.lomt"
use "log.lomt"
use "stat.lomt"
